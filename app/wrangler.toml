name = "wonderwall"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# SPA mode - deploy static client build with _redirects for fallback routing
pages_build_output_dir = "./build/client"

[vars]
VITE_CONVEX_URL = "https://courteous-rabbit-750.convex.cloud"
